interface:
  display_name: "m:autoflow execute"
  short_description: "Execute confirmed workflow tasks"
  default_prompt: "Use $m-autoflow-execute to implement confirmed plan tasks inside the active worktree."
