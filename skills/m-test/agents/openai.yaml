interface:
  display_name: "m:test"
  short_description: "Validate changes with evidence"
  default_prompt: "Use $m-test to run validation and review for confirmed workflow tasks. For UI changes, open and operate the UI, provide screenshots, and summarize results in a pass/fail table."
