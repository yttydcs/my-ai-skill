interface:
  display_name: "m:archive"
  short_description: "Archive and close staged workflows"
  default_prompt: "Use $m-archive to create docs/change records and close the workflow when confirmed."
